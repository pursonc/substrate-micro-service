syntax = "proto3";

package substrate;

service SubstrateService {
    rpc transfer(TransferReq) returns (TransferResp);
}

message TransferReq {
    string fromKey = 1;
    string to = 2;
    string amount = 3;
    string coin = 4;
}

message TransferResp {
    string signedTx = 1;
}

